interface:
  display_name: "IAM Timeseries Q&A"
  short_description: "Answer IAM PARIS questions with guided variable, region, and scenario follow-ups"
  default_prompt: "Answer IAM PARIS model metadata and time-series questions using the repo helpers and cached API data. Expect plain-language queries, suggest the top 3 relevant choices when the wording is meaningful, and ask a short follow-up when the query is too vague."
